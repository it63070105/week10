pipeline {
    agent any

    stages {
        stage('Run Shell Command') {
            steps {
                sh 'echo "Hello, world!"'
                sh 'docker ps -a'
            }
        }
    }
}
