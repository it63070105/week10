pipeline {
    agent any

    stages {
        stage('Run Shell Command') {
            steps {
                sh 'echo "Hello, world!"'
                sh 'Current directory is: pwd'
                sh 'ls'
     
                 dir('./Lab_Jenkins') {
                     sh 'move Lab_Jenkins La'
                     sh 'ls'
                 }          
           
                sh 'docker build -t myapp .'
                sh 'docker run -d -p 8085:3000 myapp'
                sh 'docker ps -a'
            }
        }
    }
}
